# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="AIzaSyDPihXJ-dNtwJr7K6Mn9OTBKsKsACdlRW4"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="https://generativelanguage.googleapis.com/v1beta/models/gemini-flash-latest:generateContent"

DB_CLIENT=pg
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=1234
DB_NAME=vms_db

# DATABASE_URL: Postgres connection string (required for production)
DATABASE_URL="postgres://user:1234@host:port/database"

# JWT_SECRET: Secret for signing JWT tokens
JWT_SECRET="22019b2fb6e554f8e6612c4ccd43c25d8d66c7280c202f3b9dc444da5e97cb4c"

# FRONTEND_URL: Used for CORS and email links
FRONTEND_URL="http://localhost:3000"

# Email Configuration
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@vmspro.com
